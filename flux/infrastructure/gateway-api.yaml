apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: gateway-api
  namespace: flux-system
spec:
  interval: 1h
  path: ./infrastructure/gateway-api
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
